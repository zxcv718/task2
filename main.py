"""프로그램 실행 진입점을 제공하는 모듈.

이 프로젝트는 기능이 여러 파일로 분리되어 있지만, 실제로 실행을 시작하는
위치는 한 곳으로 고정해 두는 편이 이해하기 쉽다. 그래서 이 파일은
`python main.py`처럼 직접 실행될 때의 시작점만 담당하고, 게임 규칙이나
상태 저장 같은 세부 로직은 모두 `QuizGame`으로 넘긴다.
"""

from quiz_game import QuizGame


def main() -> None:
    """퀴즈 게임 객체를 만들고 메인 루프를 시작한다.

    실행 진입점에서는 가능한 한 의존성을 단순하게 유지한다. 이렇게 하면
    평가자가 이 파일을 보았을 때 "객체 생성 -> 실행"이라는 전체 구조를
    가장 먼저 파악할 수 있다.
    """
    game = QuizGame()
    game.run()


if __name__ == "__main__":
    main()
